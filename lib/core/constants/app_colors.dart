import 'package:flutter/material.dart';

class AppColors {
  // Warna Utama (Primary)
  static const Color primary = Color(0xFF065F46); // Hijau Utama Finansia

  // Warna Latar & Permukaan (Background & Surface)
  static const Color background = Color(0xFFFAFAFA); // Putih bersih untuk latar
  static const Color surface = Color(
    0xFFFFFFFF,
  ); // Putih murni untuk kartu/card

  // Warna Teks (Typography)
  static const Color textPrimary = Color(0xFF0F172A); // Hitam gelap untuk judul
  static const Color textSecondary = Color(0xFF64748B); // Abu-abu untuk detail

  // Warna Status
  static const Color success = Color(0xFF10B981); // Hijau terang (Pemasukan)
  static const Color error = Color(0xFFEF4444); // Merah (Pengeluaran)
}
